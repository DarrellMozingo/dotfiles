# Current work specific commands, presumably stored in their private git

# Secure token storage on mac:
# security add-generic-password -a "$USER" -s "GitHub Token" -w $token
#export GITHUB_TOKEN="$(security find-generic-password -a "$USER" -s "GitHub Token" -w)"

#alias foo='whoami'

